@using GameStore.Data.Models
@model GameStore.Data.Models.Comment
@inject UserManager<Account> UserManager
@*<li class="media col-md-12">
        <div class="border border-primary mb-2 pt-2 pb-2 col-md-12">
            <div class="col-md-2">
                <img class="float-left align-self-start mr-3 img-fluid img-thumbnail" src="~/images/avatars/@(Model.Account.AvatarImageName ?? "default.jpg")" alt="@Model.Account.UserName">
            </div>
            <div class="media-body">
                <div class="col-md-12">
                    <h5 class="mt-0"><strong>@Model.Account.UserName</strong></h5>
                    <p>@Model.Text</p>
                    <span class="mr-1 float-right badge badge-pill badge-primary">@Model.TimeStamp</span>
                </div>
            </div>
        </div>
    </li>*@

<li class="mb-4 pl-3 pr-3">
    <div class="card">
        <div class="card-header">
            <span class="badge badge-pill badge-dark float-left">Posted On: @Model.TimeStamp</span>
            @if (await UserManager.IsInRoleAsync(Model.Account, "Admin"))
            {
                <span class="badge badge-pill badge-danger float-right">Admin Post</span>
            }
            </div>
            <div class="card-body d-flex text-truncate">
                <div class="col-md-2 float-left pl-0 text-truncate clearfix">
                    @if (await UserManager.IsInRoleAsync(Model.Account, "Admin"))
                    {
                        <div class="col-md-12 text-center pl-0 pr-0"><span class="badge badge-pill badge-danger text-truncate">@@@Model.Account.UserName</span></div>
                    }
                    else
                    {
                        <div class="col-md-12 text-center pl-0 pr-0"><span class="badge badge-pill badge-primary text-truncate">@@@Model.Account.UserName</span></div>
                    }
                    <img class="float-left align-self-start rounded img-fluid" src="~/images/avatars/@(Model.Account.AvatarImageName ?? "default.jpg")" alt="@Model.Account.UserName" />
                </div>
                <hv></hv>
                <p class="col-md-10 small">
                    @Model.Text
                </p>
            </div>
        </div>
    </li>